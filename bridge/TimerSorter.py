from datetime import datetime

class TimerSorter(Sorter):
    def timerSorter(self, list_):
        start = datetime.now()
        self.sort(obj)
        end = datetime.now()
        print("time:"+(end - start))
